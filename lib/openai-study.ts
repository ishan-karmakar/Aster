export async function explainScheduleChange(apiKey:string|undefined,reason:string,fallback:string){if(!apiKey)return fallback;try{const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{authorization:`Bearer ${apiKey}`,"content-type":"application/json"},body:JSON.stringify({model:"gpt-5.4-mini",input:`Explain this automatic student schedule change in two calm, concise sentences. State what changed and what the student should do next. Do not invent details. Reason: ${reason}`})}),data=await response.json() as {output_text?:string;output?:Array<{content?:Array<{text?:string}>}>};if(!response.ok)return fallback;return data.output_text||data.output?.flatMap(item=>item.content||[]).map(item=>item.text||"").join("")||fallback}catch{return fallback}}
