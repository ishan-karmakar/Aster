"use client";
import { useEffect,useState } from "react";
type ImportRecord={id:string;filename:string;status:string;createdAt:number;items:unknown[]};
export function ImportHistory({token}:{token:string}){const [records,setRecords]=useState<ImportRecord[]>([]);useEffect(()=>{let active=true;void fetch("/api/imports",{headers:{authorization:`Bearer ${token}`}}).then(response=>response.json()).then(data=>{if(active)setRecords(data.imports||[])});return()=>{active=false}},[token]);async function remove(id:string){await fetch(`/api/imports/${encodeURIComponent(id)}`,{method:"DELETE",headers:{authorization:`Bearer ${token}`}});setRecords(current=>current.filter(item=>item.id!==id))}if(!records.length)return null;return <div className="settings-card"><div className="settings-card-head"><span className="settings-class-icon">↻</span><div><h2>Import history</h2><p>Retry an upload above or delete its private source file here.</p></div></div>{records.map(record=><div className="integration-card" key={record.id}><div><h3>{record.filename}</h3><p>{record.status} · {record.items.length} extracted items · {new Date(record.createdAt*1000).toLocaleString()}</p></div><button className="ghost-button" onClick={()=>void remove(record.id)}>Delete</button></div>)}</div>}
